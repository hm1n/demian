# 서비스 개요

GitHub Repository의 코드와 커밋 이력을 분석해 설명할 가치가 있는 개발 경험을 선별하고, 실제 코드 기반 AI 인터뷰로 사용자가 그 경험을 스스로 설명하도록 돕는 서비스입니다.

핵심 흐름은 Repository 분석, 경험 선별, 근거 확인, AI Interview, 경험 구체화 순서로 진행됩니다. AI는 이력서나 코드 설명을 대신 작성하지 않습니다. AI는 항상 실제 Repository의 커밋과 파일을 근거로 제시하며, Repository만으로 확인할 수 없는 사실은 명확히 구분해서 보여줘야 합니다.

# 핵심 기술 챌린지

이 서비스의 프론트엔드 핵심 챌린지는 AI Streaming 환경에서 Markdown과 Code Block이 포함된 긴 대화의 렌더링 성능과 스크롤 UX를 안정적으로 유지하는 것입니다. Streaming 중 리렌더링 범위를 최소화해야 합니다. 메시지가 누적될 때 DOM 증가와 렌더링 비용을 관리해야 합니다. Syntax Highlighting과 Markdown Parsing 비용을 관리해야 합니다. 사용자의 수동 스크롤과 자동 스크롤이 충돌하지 않도록 처리해야 합니다.

# MVP 범위

MVP 핵심 흐름은 Repository 입력, Repository 데이터 조회, 경험 후보 생성, 경험 후보별 근거 표시, 경험 선택, 코드 기반 AI 질문, 사용자 답변, 실시간 Streaming 응답, 꼬리 질문까지입니다. 이 범위를 벗어나는 기능은 먼저 논의 없이 추가하지 않습니다.

# 인터뷰 설계 원칙

AI가 던지는 질문은 항상 실제 코드나 커밋을 근거로 삼아야 합니다. 사용자의 답변을 바탕으로 기술 선택의 이유와 근거를 설명하도록 꼬리 질문을 생성해야 합니다. 단순 암기나 일반론으로 답할 수 있는 질문은 지양합니다.

# 작업 원칙

코드를 작성하거나 리뷰할 때는 항상 실제 성능 측정을 근거로 최적화 여부를 판단해야 합니다. 추측성 최적화를 먼저 적용하지 않습니다. Loading, Empty, Error 상태는 모든 핵심 흐름에서 항상 함께 구현해야 합니다. 자세한 배경, 진행 상황, 세부 결정 사항은 이 문서에 적지 않고 llm-wiki에 기록하고 갱신합니다.
