# 서비스 개요

GitHub Repository의 코드와 커밋 이력을 분석해 설명할 가치가 있는 개발 경험을 선별하고, 실제 코드 기반 AI 인터뷰로 사용자가 그 경험을 스스로 설명하도록 돕는 서비스입니다.

핵심 흐름은 Repository 분석, 경험 선별, 근거 확인, AI Interview, 경험 구체화 순서로 진행됩니다. AI는 이력서나 코드 설명을 대신 작성하지 않습니다. AI는 항상 실제 Repository의 커밋과 파일을 근거로 제시하며, Repository만으로 확인할 수 없는 사실은 명확히 구분해서 보여줘야 합니다.

# 핵심 기술 챌린지

이 서비스의 프론트엔드 핵심 챌린지는 AI Streaming 환경에서 Markdown과 Code Block이 포함된 긴 대화의 렌더링 성능과 스크롤 UX를 안정적으로 유지하는 것입니다. Streaming 중 리렌더링 범위를 최소화해야 합니다. 메시지가 누적될 때 DOM 증가와 렌더링 비용을 관리해야 합니다. Syntax Highlighting과 Markdown Parsing 비용을 관리해야 합니다. 사용자의 수동 스크롤과 자동 스크롤이 충돌하지 않도록 처리해야 합니다.

# MVP 범위

MVP 핵심 흐름은 Repository 입력, Repository 데이터 조회, 경험 후보 생성, 경험 후보별 근거 표시, 경험 선택, 코드 기반 AI 질문, 사용자 답변, 실시간 Streaming 응답, 꼬리 질문까지입니다. 이 범위를 벗어나는 기능은 먼저 논의 없이 추가하지 않습니다.

# 인터뷰 설계 원칙

AI가 던지는 질문은 항상 실제 코드나 커밋을 근거로 삼아야 합니다. 사용자의 답변을 바탕으로 기술 선택의 이유와 근거를 설명하도록 꼬리 질문을 생성해야 합니다. 단순 암기나 일반론으로 답할 수 있는 질문은 지양합니다.

# 작업 원칙

코드를 작성하거나 리뷰할 때는 항상 실제 성능 측정을 근거로 최적화 여부를 판단해야 합니다. 추측성 최적화를 먼저 적용하지 않습니다. Loading, Empty, Error 상태는 모든 핵심 흐름에서 항상 함께 구현해야 합니다. 자세한 배경, 진행 상황, 세부 결정 사항은 이 문서에 적지 않고 llm-wiki에 기록하고 갱신합니다.

## Code Review Rules

리뷰 배경과 각 규칙이 생긴 경위는 `llm-wiki/wiki/2026-08-20-코드리뷰-전략.md`에 있습니다.

### 지적 범위

지적한 줄만 보고하지 않습니다. 같은 함수와 같은 데이터 흐름 안에 같은 성격의 결함이 더 있으면 함께 보고합니다. 한 가지 근본 원인이 여러 지점에서 드러나는 경우에는 그 지점들을 하나로 묶어서 보고합니다.

### 외부 API 호출

외부 API를 호출하는 코드는 성공 응답 외에 404, 409, 403, 429, 422 응답을 각각 어떻게 처리하는지 확인합니다. 처리하지 않은 상태 코드가 있으면 지적합니다.

GitHub API는 다음 동작을 전제로 검토합니다. 빈 저장소는 커밋 목록 API에서 409를 반환합니다. 빈 저장소는 브랜치 조회 API에서 ref가 없으므로 404를 반환합니다. 저장소 `size` 필드는 KB 단위 저장 용량이므로 커밋 유무를 판별하는 기준으로 쓸 수 없습니다.

rate limit은 세 가지를 구분해서 처리해야 합니다. 429는 primary입니다. 403에 `x-ratelimit-remaining: 0` 헤더가 있으면 primary입니다. 403에 `Retry-After` 헤더가 있거나 본문 메시지에 `secondary rate limit` 또는 `abuse detection`이 있으면 secondary입니다. primary만 처리하고 나머지를 인증 실패로 분류하는 코드는 지적합니다.

### 응답 파싱

`response.json()` 호출은 파싱 실패를 전제로 try/catch로 감싸고 타입이 있는 에러로 변환해야 합니다. 감싸지 않은 호출, 그리고 파싱 실패 때문에 이미 수집한 결과를 버리는 코드는 지적합니다.

### 페이지네이션

페이지네이션 커서로 브랜치명처럼 조회 도중 이동할 수 있는 ref를 쓰지 않습니다. 커밋 SHA처럼 한 번 고정한 값을 씁니다. 이동하는 ref를 쓰면 페이지 경계에서 항목이 중복되거나 누락될 수 있으므로 지적합니다.

### 호출 순서

방어 코드를 추가할 때 그 코드가 실제로 도달 가능한지 확인합니다. 앞 단계가 먼저 에러를 던져서 뒤 단계의 예외 처리가 도달 불가능해지는 경우를 지적합니다.

### 판별 기준

어떤 상태를 판별할 때 그 상태를 직접 나타내지 않는 대리 지표를 쓰지 않습니다. 대리 지표로 판별하는 코드는 반례를 함께 들어 지적합니다.

### 테스트

지적한 결함마다 그 결함을 재현하는 테스트가 있는지 확인합니다. 테스트 없이 수정만 반영된 경우 회귀 테스트 추가를 요청합니다.
